To build the exe for the mod and for the python app (include frontend) `server/build.ps1` 

Then to make the installer open Inno Setup with `tools/SkyBridgeSetup.iss` and click compile.

Then the installer will be in `tools/installer/.exe` and you can run it to install the mod and the python app.
